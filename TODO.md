# TODO

## Before Publishing

- [ ] Fill in real project content and links (Project Two card + both `link: '#'` placeholders in JS)
- [x] Add `<meta name="description">` and favicon to `<head>`
- [x] Add `@media (prefers-reduced-motion: reduce)` guards on animations (fade-in, hover, preview, panel)
- [x] Make cards keyboard-accessible: add `role="button"` and `tabindex="0"` to `.glass-card` divs
- [ ] Revisit chip design on project cards and panel (e.g. Archived chip styling, placement, and whether other chip types are needed)

## Low Priority

- [x] Hover preview clips on narrow viewports (< ~680px) — extended hide breakpoint to 679px
- [x] Remove or update stale spec file at `specs/2026-04-04-add-page-sections.md`
- [x] Remove orphaned `terminal.html`
