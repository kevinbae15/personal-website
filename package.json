{
  "scripts": {
    "build": "tailwindcss -i input.css -o output.css --minify",
    "watch": "tailwindcss -i input.css -o output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
